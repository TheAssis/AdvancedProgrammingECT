/*

a primeira função main é responsável por mostrar os elementos do vetor.
 Isso ocorre pois o comando <printf("%d ",*(vet+i));>  mostra termos
 inteiros guardados na posição vet+i, devido a existência do asterisco,
 além de quê vet significa a posição 0.

a segunda função main é responsável por mostrar os endereços dos elementos
do vetor. Isso ocorre devido a ausência do asterisco.


*/
